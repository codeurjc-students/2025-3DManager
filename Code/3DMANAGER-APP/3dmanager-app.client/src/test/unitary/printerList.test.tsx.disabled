//Commented test because its not useful with teh real proyect
/*
import { render, screen } from '@testing-library/react'
import App from '../../App'



test('renders app title', () => {
    render(<App />)
    expect(screen.getByText(/3D Manager/i)).toBeInTheDocument()
})
*/