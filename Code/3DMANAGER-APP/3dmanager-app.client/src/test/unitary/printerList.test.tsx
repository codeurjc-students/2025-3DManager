//Comento este test por que deja de tener sentido una vez comenzado el proyecto real
/*
import { render, screen } from '@testing-library/react'
import App from '../../App'



test('renders app title', () => {
    render(<App />)
    expect(screen.getByText(/3D Manager/i)).toBeInTheDocument()
})
*/